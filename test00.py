#!/usr/bin/env python
import tensorflow as tf
import numpy as np
import math
#import csv
import matplotlib.pyplot as plt
import time
import matplotlib.animation as animation
import keras

# a = [0]
# print([a[0] +1])

print([[2]] < [[0]])
# #plt.ion()
# a = list(range(1,20,4))
# print(a)
# x_points = [0, 1, 2, 3, 4, 5]
# y_points = [0, 1, 2, 3, 4, 5]
#
# fig = plt.figure()
# ax = plt.axes(xlim=(-5, 6), ylim=(-2, 6))
# particles, = ax.plot([], [], 'bo', lw=2)
#
#
#
# def init():
#     # initiate animation
#     particles.set_data([], [])
#     return particles
#
#
# def animate(i):
#     y = []
#     x = []
#     y.append(y_points[i])
#     x.append(x_points[i])
#     particles.set_data(x, y)
#
#     return particles
#
#
# print('start anim')
#
# anim = animation.FuncAnimation(fig, animate, np.arange(0,5),
#                                interval=100, init_func=init, repeat=False)
# plt.show()

# a = np.array([[2,1],[4,3]])
# b = np.array([2,1], ndmin=2)
# c = np.equal(a, b)
# d = np.where(c)
# print(c)
# a = tf.constant(np.array([[2,1],[2,3]]))
# b = tf.constant(np.array([2,1], ndmin=2))
# c = tf.equal(a, b)
# d = tf.where(c)
#
#
# # Before starting, initialize the variables.  We will 'run' this first.
# init = tf.initialize_all_variables()
#
# # Launch the graph.
# sess = tf.Session()
# sess.run(init)
#
# sess.run(d)
# print(d)

